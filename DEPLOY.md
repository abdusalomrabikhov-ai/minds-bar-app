# Деплой TeamTask

Стек: Node.js >=22, Express, SQLite (`teamtask.db`), хостинг — Railway (builder: NIXPACKS).

## Команда деплоя

```bash
./deploy.sh
```

Скрипт — обёртка над `railway up --detach`.

**Важно:** деплой (`railway up`) запускать только по явной команде юзера. Не деплоить проактивно после правок.

## Перед деплоем

- Прогнать тесты: `npx playwright test`
- Если менялся CSS **или JS** в `public/` — бампнуть `?v=N` в `index.html` для соответствующего файла (кэш-бастинг, assets кэшируются `immutable` на год). Текущие версии: `style.min.css?v=39`, `app.min.js?v=25`
- Если менялся `public/js/app.js` или `public/css/style.css` — прогнать `npm run build` (esbuild минификация → `.min.js`/`.min.css`), т.к. `index.html` грузит минифицированные версии, не исходники
- Проверить, что `server.js` стартует локально (`npm start`) без ошибок

## После деплоя

```bash
railway logs --tail   # логи в реальном времени
railway status        # статус деплоя
railway open           # открыть сайт в браузере
```

## Конфиг

- `railway.toml`: `startCommand = "node server.js"`, restart policy — ON_FAILURE, max 3 retries
- `package.json`: `start` → `node server.js`, `dev` → `nodemon server.js`
- Домен: `bar.mindstech.io` (CNAME → Railway) подключён к сервису minds-bar-app

## Deploy log

(новые записи сверху)

- 2026-07-22 — fix: дедлайн задач из контент-плана ставится 18:00, а не голая дата (7552be8). `syncTasksForItem` (server.js) писала `tasks.deadline = item.date` (`YYYY-MM-DD` без времени) при создании/обновлении задачи из поста/рилса/сториса — время эффективно было «случайным» (23:59:59 для просрочки, UTC-полночь для отображения). Фикс: новая переменная `deadline = item.date + 'T18:00:00'`, используется в обоих write-путях (INSERT новой задачи и UPDATE при `syncDeadline`). Сама таблица `content_plan.date` не тронута. Только server.js, JS/CSS не менялись — без cache-bust. Первая попытка `railway up` упала с `500 Internal Server Error` при аплоаде (сторона Railway), вторая попытка прошла успешно.
- 2026-07-22 — fix: сводные отчёты «Месяц» не совпадали с дашбордом (8da8482). `buildSummaryData` (`reports.js`) фильтровала период по `created_at` скользящим окном 30 дней; дашборд/карточка сотрудника — по `deadline` (fallback `created_at`) календарным месяцем. Отсюда расхождение всех метрик за «Месяц» (просрочено 3 vs 8, всего 850 vs 458 и т.д.). Фикс: при `days===30` без `from`/`to` окно = календарный месяц текущей даты; все запросы (global/per-employee/dlRows/taskList) переведены на то же поле дат, что дашборд. Остальные периоды (7/14/90/180/365 дней) остались rolling — без изменений. Только server-side `reports.js`, JS/CSS не менялись — без cache-bust. Задеплоено (`railway up`), Online, bar.mindstech.io отвечает.
- 2026-07-21 — fix: дедлайн контент-план задач откатывался после сохранения + admin автоправа (d8d4776). Ре-рендер страницы проекта дёргал `sync-content-tasks` для всех content-plan айтемов, та безусловно перезаписывала `tasks.deadline` из `content_plan.date` — только что сохранённый через `PUT /api/tasks/:id` дедлайн откатывался обратно, без записи в `task_history` (баг pre-существовал, стал заметен после lockdown-фичи от 07-17). Фикс: `syncTasksForItem` получила `syncDeadline` флаг, `sync-content-tasks` больше не трогает дедлайн — только `PUT /api/content/:id`/импорт. Заодно: оба admin-аккаунта в БД получили все 12 permissions (бэкенд и так давал admin полный доступ через `can()`, но UI-бейджи прав были пустые); новый `ALL_PERMS`/`permsForRole()` — новый admin создаётся сразу со всеми правами, существующего admin нельзя урезать через форму. Только server.js, CSS/JS не менялись — без cache-bust. Задеплоено (`railway up`), Online, логи чистые (сервер стартовал без ошибок, TG-бот webhook поднялся).
- 2026-07-18 — fix: `app.set('trust proxy', 1)` за прокси Railway (a95c3c6). До этого rate-limit логина (10 попыток/15 мин) считал всех по IP прокси — один общий лимит на всю команду; в логах сыпался `ERR_ERL_UNEXPECTED_X_FORWARDED_FOR`. Найдено при проверке логов после деплоя v33.
- 2026-07-17 — feat: lockdown редактирования (c840b08) — против тихих переносов сроков. Дедлайн существующей задачи меняют только админ + держатель «Проверки контент-плана» (проверяющий добавлен во входной гейт `PUT /api/tasks/:id`); менеджеры (`assign_tasks`/`manage_team`) — все поля кроме дедлайна; рядовой исполнитель — только статус (сравнение полей со старыми значениями, модалка шлёт полный payload). Право «Проекты» — «только добавлять» в контент-плане: добавить пост/рилс/сторис + импорт Excel можно, `PUT/DELETE /api/content/:id` и очистка месяца — только админ. Frontend: чипы КП read-only для не-админов, «Редактировать» скрыт для рядовых, datepicker дедлайна disabled в edit-режиме. Создание задач с дедлайном — всем как раньше. app.min.js?v=33. Проверено до деплоя: 15/15 API-тестов lockdown + 13/13 регресс review-фичи + smoke 26/26; после — v33 live, маршруты 401 без токена, TG-бот запущен (webhook).

- 2026-07-17 — feat: право «Проверка контент-плана» (`review_content_plan`) — глобальный ревью выполненных задач проектов + Telegram о смене дедлайнов. Держатель права: все задачи с `project_id`, закрытые обычным сотрудником (включая авто-задачи контент-плана из Excel-импорта), уходят в `pending_review`; видит полную очередь в «Проверке», approve/reject любой задачи; уведомления in-app+SSE+TG при попадании на проверку и при любой смене дедлайна (кто, старый → новый срок). Админ/сам проверяющий/задачи без проекта — без проверки; нет держателей права — гейт выключен. Старый creator-флоу `review_tasks` не тронут. Попутный фикс: миграция `users.archived` шла до `CREATE TABLE users` — свежая БД падала 500 на `/api/users` (прод не был задет). Проверено до деплоя: 13/13 API-тестов на изолированной БД + smoke 26/26. app.min.js?v=32. Задеплоено (fa6effd), Online — проверено: bar.mindstech.io отдаёт v32, API отвечает. TG-доставка проверяется только вживую на проде — прогнать руками.

- 2026-07-15 — feat/fix: подзадачи (чек-лист) для задач + 3 бага, все задеплоены вместе. (1) Подзадачи: опциональный чек-лист на задаче, блокирует "Готово"/approve пока не выполнены все пункты. Новая таблица `subtasks`, CRUD `/api/tasks/:id/subtasks`, `/api/subtasks/:id`, гейт в PUT/my-done/approve, копируются в рекуррентные задачи. Впервые прогнано вживую через Playwright e2e (29/29 smoke зелёные) — раньше (2026-07-13) браузерный тулинг был недоступен, проверялось только через curl. (2) e2e `.env.test` тихо перебивал `.env` — весь smoke-набор на самом деле шёл на прод под живым логином, не на localhost; `env-config.ts` теперь требует `TEST_ENV=1` для `.env.test`. (3) Аватары без ресайза раздували HTML — 8 юзеров грузили base64 до 909KB, вставлялось напрямую в 15+ мест, дашборд рендерил 38MB HTML. Клиент теперь ресайзит до 256px JPEG через canvas перед аплоадом, серверный лимит 2MB→300KB. (4) Двойной клик "Отправить" на комментарии к задаче создавал 2 одинаковых комментария — та же причина, что у календаря (9a7ee04): кнопка не блокировалась на время запроса. app.min.js?v=31, style.min.css?v=44. Задеплоено, Online — проверено: bar.mindstech.io 200, отдаёт v31/v44.
- 2026-07-10 — feat: вкладка "Поручил" в "Задачи для проверки" — админ/менеджер с правом `review_tasks` видит все задачи, которые лично создал и назначил сотрудникам (любой статус, дефолт "кроме готовых"), не только `pending_review`. Новый `GET /api/tasks/assigned-by-me` (server.js), та же схема прав что у `pending-review`. Фронт: `renderReviewPage()` стал 2-табовым диспетчером, переиспользует существующие `taskCard()`/tab-бар/filter-btn компоненты, новых цветов/CSS не вводили. Доступ per-user — каждый видит только свои поручения (`created_by = текущий юзер`), не чужие. app.min.js?v=29. Задеплоено, Online — проверено: bar.mindstech.io 200, отдаёт v29, /api/tasks/assigned-by-me отвечает (401 без токена, не 404 — роут существует).

- 2026-07-10 — design: закрыт последний кластер token-drift. Заменён недоделанный ребренд-остаток danger-red #dc2626/#DC2626 → var(--danger)/#E5484D (97 мест в app.js/style.css: overdue-индикаторы, debt/expense-цвета, danger-кнопки, urgency-border). Indigo #6366F1 (~28 мест) оказался не drift, а намеренный category-цвет (default аватар, B2C-идентити) — задокументирован как новый токен в DESIGN.md/design.json, код не менялся. style.min.css?v=43, app.min.js?v=28. Задеплоено, Online — проверено: bar.mindstech.io 200, отдаёт v43/v28, /tasks и /finance проверены вживую.
- 2026-07-10 — design: установлен impeccable design-skill, пересобраны PRODUCT.md/DESIGN.md из реального источника (public/css/style.css), critique dashboard. Фиксы: заменён недоделанный ребренд-остаток burgundy #881337/#881237 → #8E1B3B/#B4234C (41 место в app.js/style.css), нетокенизированный серый #9ca3af → var(--text-light) (91 место), .cp-task-badge перекрашен с зелёного на нейтральный (конкурировал со статус-бейджами на task-карточках). style.min.css?v=42, app.min.js?v=27. Задеплоено, Online — проверено: bar.mindstech.io 200, отдаёт v42/v27.
- 2026-07-08 — fix: 6 багов после perf-аудита, 6 коммитов, все задеплоены и проверены live. (1) Табель: sync без UNIQUE constraint на user_id дублировал сотрудников на каждой загрузке страницы — прод накопил 455 строк вместо 17. Добавлена one-time дедупликация в initDB() (оставляет строку с наибольшим числом timesheet_records, удаляет остальные) ПЕРЕД созданием UNIQUE index — предыдущий фикс (c3197ae, тот же день) сам индекс не мог создаться на уже задублированных данных, ошибка тихо глоталась в catch{} (commit c3197ae). (2) iPhone: шапка уходила под статус-бар/Dynamic Island — apple-mobile-web-app-status-bar-style:black-translucent требует safe-area-inset-top padding на .topbar, которого не было (commit 5c26747). (3) Уведомления о дедлайне: date-only дедлайн ("2026-07-08" без времени) сравнивался как голая строка — совпадало весь день, слало "меньше часа" хоть в 10 утра. Теперь сравнивается с концом дня (23:59:59), как и фронтенд уже считает (commit 548f3cd). (4) Admin не мог отметить "Готово" за чужую single-assignee задачу в detail-модалке — условие показа кнопки не включало isAdmin (мульти-assignee ветка уже это умела) (commit a605749). (5) Легаси-данные: 157 задач имели осиротевшую 1-строчную запись в task_assignees (из эпохи до single-vs-multi разделения) — рендерили чеклист-UI вместо обычного "Готово"-UI, визуально путая. One-time cleanup в initDB() удаляет task_assignees для task_id с ровно 1 записью (commit 899f9a5). (6) Двойной клик "Сохранить" при создании/редактировании календарного события создавал 2 отдельных события + 2 независимых telegram-уведомления — кнопка не блокировалась на время запроса (commit 9a7ee04). style.min.css?v=39, app.min.js?v=25.
- 2026-07-08 — perf: полный аудит производительности + PWA + Google-Calendar-style виды календаря (Day/4day/Week/Year/Agenda). Backend: CORS сужен до /api (было глобально, блокировало same-origin font-запросы), async bcrypt, N+1 fix в content-plan sync, 3 новых индекса (finance_history/push_subscriptions/finance_activity_log), TTL-кэш на dashboard/reports/best-employee/workload, LIMIT на /api/tasks?all=1, UNIQUE index на timesheet_employees.user_id (чинит баг с дублирующимися сотрудниками в Табеле — sync создавал новую строку на каждой загрузке страницы). Frontend: esbuild минификация (npm run build), Gotham-шрифты 8×OTF/TTF (~1MB) → 2×WOFF2 (~106KB), debounce на поиске задач, PWA iOS meta-теги + maskable icon + SW cache-first для versioned assets. Календарь: новый view-switcher (день/4дня/неделя/месяц/год/расписание), time-grid rendering с overlap-алгоритмом, drag-drop, dark mode + mobile (commit b3049ce). style.min.css?v=38, app.min.js?v=23. Задеплоено, Online — проверено: bar.mindstech.io и railway domain оба 200, логи чистые, /api/timesheet/employees возвращает 18 без дублей после 3 повторных вызовов.
- 2026-07-07 — fix: полный security-аудит, 4 коммита. (1) КРИТИЧНО: IDOR в GET /api/tasks/:id (canAccessTask не вызывалась), XSS через title/name в 12 местах (_escHtml добавлен), удалён мёртвый seed-эндпоинт с реальными bcrypt-хешами 16 аккаунтов (6a00f5d). (2) ВАЖНО: JWT_SECRET fatal exit вместо fallback, JWT 30d→7d, global error handler, DELETE user — friendly ошибка вместо FK crash, mobile touch targets 44px (c42135c). (3) CORS ограничен до bar.mindstech.io + railway domain, Telegram token TTL 15мин + webhook secret (опционально), транзакции на project/task delete, 5 недостающих индексов (eff0e5d). (4) Удалён мёртвый Google OAuth код + googleapis зависимость (91f8cdd). app.js?v=16, style.css?v=30. Задеплоено, Online — проверено: сервер стартовал без crash (JWT_SECRET/NODE_ENV подтверждены в Railway env), login работает, CORS preflight корректен для bar.mindstech.io и railway domain.
- 2026-07-07 — fix: удаление occurrence повторяющегося календарного события ("это и все последующие") удаляло всю серию. Root cause: все occurrences повторяющегося события расширяются на лету с одинаковым id — openCalEventDetail(eventId) матчил только по id, всегда резолвя первое вхождение независимо от того на какую дату кликнул юзер. Теперь матчит по id+start.dateTime (commit ac7656b). app.js?v=15. Задеплоено, Online.
- 2026-07-07 — fix: повторяющиеся задачи не спавнили следующую через checklist (my-done) и approve-пути, только через обычный PUT-статус — вынесено в shared spawnRecurringTask(). Root cause: одиночный исполнитель ошибочно создавал task_assignees запись (мультизадачный формат), теряя обычный статус-flow — setTaskAssignees() теперь только для 2+. Добавлена кнопка "Готово" для одиночных задач (toggleTaskDone), автозакрытие detail-модалки при финальном статусе, deleteTask/save-задачи теперь обновляют и страницу "Мои задачи" (commit 7c58b9d). app.js?v=14. Задеплоено, Online.
- 2026-07-07 — fix: описание задачи/календарного события теряло переносы строк при отображении (white-space:pre-wrap отсутствовал) — также экранирование добавлено для task description (не было вообще, XSS-риск). Отдельно: все 16 мест с нативным browser confirm() заменены на кастомную модалку showConfirmModal() — финансы Kids/B2C (удаление курса/студента), задачи, проекты, content-plan, расписание, feedback, telegram (commit e6f26eb). app.js?v=11. Задеплоено, Online.
- 2026-07-06 — fix: глобальный поиск (/api/search) не находил задачи с заглавными кириллическими буквами в названии — SQLite LOWER() ASCII-only, не лоуеркейзит кириллицу. Добавлена unicode-aware SQL-функция lower_u() (commit c0c5985). Задеплоено, Online.
- 2026-07-06 — fix: мобильные модалки не реагировали на скролл (touch проваливался на body под модалкой) — body.style.overflow теперь блокируется/восстанавливается при открытии/закрытии. Также бампнуты cache-busting версии app.js (v=9) и style.css (v=29) — раньше правки от сегодняшних более ранних фиксов не долетали до браузеров из-за immutable-кэша (commit 3f323e1). Задеплоено, Online.
- 2026-07-06 — fix: PWA-иконки заменены на wordmark-лого "minds bar" вместо старого equalizer-логотипа (commit 4903d37). Задеплоено, Online.
- 2026-07-06 — feat: PWA install icons (192/512/apple-touch), iOS safe-area fix (viewport-fit=cover), Electron desktop wrapper добавлен (commit b6f9d9d). desktop/ не деплоится на сервер — только public/+server.js. Задеплоено, Online.
- 2026-07-06 — fix: SSE-события (в т.ч. "Задачи для проверки") задерживались из-за глобального compression() middleware, буферизовавшего /api/events. Исключён из сжатия + refreshCurrentPage() теперь освежает страницу review (commit 0468d47). Задеплоено, Online на bar.mindstech.io.
- 2026-07-06 — fix: review-gate игнорировал review_tasks permission, проверял только role==='admin' (commit 724f2c2). Деплой на Railway (minds-bar-app), билд запущен.
- 2026-07-06 — создан DEPLOY.md/MEMORY.md, деплоя не было
