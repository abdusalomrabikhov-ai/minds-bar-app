# Заметки проекта TeamTask

Файл заметок по решениям и договорённостям проекта. Обновлять при значимых изменениях.

## Инструменты

- **2026-07-10**: закрыт 1 из 3 оставшихся token-drift кластеров — серый `#9ca3af`. 91 место в `public/js/app.js`/`public/css/style.css` использовали нетокенизированный `#9ca3af` (color distance до `--text-light` #9B9BA8 = 10.7, до `--text-muted` = 86.8 — уверенный матч). Все контексты — loading/empty-state/placeholder текст, соответствует роли `--text-light`. Инлайновые `style="color:..."` заменены на `var(--text-light)`; SVG-атрибуты `fill=`/`stroke=` (не поддерживают `var()` вне `style=`) и data-URI SVG заменены на литерал `#9B9BA8`. Детектор: 629→566 находок. Пересобрано, cache-bust v41→v42/v26→v27, проверено вживую. Остались 2 кластера: старый красный `#dc2626` (~62×, нужен тот же burgundy-анализ — где легитимно, где drift) и indigo `#6366f1` (~28×, возможно намеренная категория цвета для B2C).
- **2026-07-10**: частично закрыт P1 (badge-перегруженность task-карточек) из dashboard-critique. `.cp-task-badge` ("Контент-план" плашка) был зелёным (#F0FDF4/#16A34A/#BBF7D0), конкурировал за внимание с priority/status/overdue-бейджами — это metadata (откуда задача), не статус/срочность. Перекрашен в нейтральный (`var(--surface-2)`/`var(--text-muted)`/`var(--border)`). Пересобран CSS-бандл, cache-bust v40→v41, проверено вживую на `/tasks`. Остальные сигналы на карточке (priority, project, status, overdue-countdown, border-left) оставлены — все они реально статус/срочность, не декорация.
- **2026-07-10**: закрыт P1 (token drift) из dashboard-critique — исправлен только кластер старого добрендового burgundy. Детектор на реальном источнике (`public/js/app.js`, `public/css/style.css`) дал 656 находок (не 104, как на устаревшем файле). Из них 41 место (25 в JS, 16 в CSS) использовали старый цвет `#881337`/`#881237`/`rgba(136,19,55,...)` вместо текущего primary `#8E1B3B`/`#B4234C`/`rgba(142,27,59,...)` — реальный недоделанный ребренд (avatar/dot fallback-цвета, chart-линии, focus-ring, drag-over состояния). Заменено везде через `sed`, пересобран `public/js/app.min.js`+`public/css/style.min.css` (`node build.js`), version bump в `public/index.html` (css v39→40, js v25→26), проверено вживую в браузере. Остальные 3 кластера drift (серый `#9ca3af` ~93×, старый красный `#dc2626` ~62×, недокументированный indigo `#6366f1` ~28×) **не трогали** — только задокументированы в детекторе, решение по ним не принято.
- **2026-07-10**: `DESIGN.md`/`.impeccable/design.json` **пересобраны из верного источника**. Первая версия (та же дата, раньше) была построена по ошибке из `css/style.css` — orphaned/устаревший файл (Jun 20, burgundy #881337, тёмный sidebar #111827), который приложение больше не использует. Реально отдаётся `public/css/style.css` (3727 строк, гораздо новее): primary burgundy `#8E1B3B`, **светлый** sidebar (не тёмный), нет border-left на active nav, есть полноценная `[data-theme="dark"]`, radius-scale 8/12/20. `/impeccable critique dashboard` на неверном спеке чуть не привёл к ненужным правкам — burgundy-градиентная "Выполнено"-карточка и border-left на task-карточках (urgency-индикатор) оказались легитимными, намеренными паттернами, а не нарушениями. Правило на будущее: перед `/impeccable document`/`critique` всегда проверять `public/index.html` → какой именно CSS/JS реально грузится (`?v=N` версия в `<link>/<script>`), а не читать первый попавшийся файл с похожим именем в корне.
- **2026-07-10**: установлены design skills — `emil-design-eng`/`animation-vocabulary`/`review-animations` (global, ~/.claude/skills) и `impeccable` (project, .claude/skills/impeccable). Прогнан `/impeccable init` + `document` + `critique dashboard`: созданы `PRODUCT.md` (register=product, platform=web), `DESIGN.md`, `.impeccable/design.json`, критика сохранена в `.impeccable/critique/`. Причина: чтобы будущие design-команды (`critique`/`polish`/`audit` и т.п.) и AI-агенты знали бренд-контекст и не ломали существующую систему.

## Функциональность

- **2026-07-17**: право «Проверка контент-плана» (`review_content_plan` в permissions JSON, без миграций). Держатель — глобальный проверяющий: любая задача **с project_id**, закрытая обычным сотрудником (включая авто-задачи контент-плана из Excel-импорта), уходит в `pending_review` вместо `done`; админ/сам проверяющий/задачи без проекта закрываются сразу; при нуле активных держателей права гейт выключен. Держатель видит ВСЕ pending_review в «Проверке» (не только свои созданные), может approve/reject любую. Уведомления держателям (in-app+SSE+Telegram, helpers `contentReviewers()`/`notifyContentReviewers()` в server.js рядом с `requirePerm`): при попадании задачи на проверку и при **любой смене дедлайна** (кто, задача, старый→новый срок — для контроля тихих переносов). Гейты в `PUT /api/tasks/:id` и `PATCH .../my-done`, `updateReviewBadgeSSE()` теперь зовётся и из single-assignee пути. Прежний creator-флоу `review_tasks` не тронут (регресс подтверждён). Frontend: ключ в `PERM_LABELS` + гейт nav-review, bump `app.min.js?v=32`. Попутно исправлен баг свежей установки: миграция `ALTER TABLE users ADD COLUMN archived` в database.js шла ДО `CREATE TABLE users` → на новой БД колонки не было и `/api/users` падал 500 (прод не задет — колонка там давно есть); перенесена в блок Migrations. Проверено: 13/13 API-тестов на изолированной БД (оба гейта, очередь, approve/reject, исключения, уведомления, регресс creator-флоу и my-done), smoke 26/26 (3 падения — окружение: тестовый порт 3799 не в CORS-allowlist + пустая БД). Деплой не выполнялся. (Playwright e2e-набор появился в репо между сессиями) — 29/29 smoke-тестов зелёные. По пути нашлись и исправлены 2 не связанных с подзадачами бага: (1) **e2e `.env.test` тихо перебивал `.env`** в `e2e/helpers/env-config.ts` — весь smoke-набор на самом деле шёл на прод (`railway.app`) под живым логином `abdusalom@minds.com`, а не на localhost; исправлено — `.env.test` теперь грузится только при `TEST_ENV=1`, `.env`/`.env.local` всегда побеждают. (2) **Аватары без ресайза раздували HTML** — 8 юзеров грузили `avatar_img` как base64 без сжатия (до 909KB на файл), а он вставляется как data-URI напрямую в 15+ мест (дашборд/команда/задачи); дашборд реально рендерил 38MB HTML на одной странице. Фикс: клиент ресайзит до 256px JPEG через canvas перед аплоадом (`resizeImageFile`, app.js рядом с `updateSidebarAvatar`), серверный лимит `POST /api/profile/avatar` ужесточён с 2MB до 300KB. Существующие раздутые аватары в локальной `teamtask.db` пересжаты тем же способом (прод не трогали).
- **2026-07-13**: подзадачи (чек-лист) для задач — опциональная функция, задачу нельзя завершить пока не выполнены все подзадачи. Новая таблица `subtasks` (task_id FK cascade, text, done, done_at, order_idx). Backend: `attachSubtasks`/`enrichTasksWithSubtasks`/`hasIncompleteSubtasks`/`blockIfIncompleteSubtasks` (server.js, рядом с `recomputeTaskStatus`), новые CRUD `GET/POST /api/tasks/:id/subtasks`, `PATCH/DELETE /api/subtasks/:id` (те же права `canAccessTask`, что у комментариев). Гейт на "done" добавлен в `PUT /api/tasks/:id`, `PATCH /api/tasks/:id/my-done`, `POST /api/tasks/:id/approve` — возвращают 400 `{code:'SUBTASKS_INCOMPLETE'}`. Рекуррентные задачи (`spawnRecurringTask`) копируют подзадачи как невыполненные заново. Frontend: секция подзадач в модалке создания (`openTaskModal`, только при создании — не в режиме правки, чтобы не было двух конкурирующих UI), секция + гейтинг кнопки "Готово" в деталях задачи (`openTaskDetail`), бейдж прогресса на карточке (`taskCard`, по образцу `ma-progress`). Проверено вживую через реальные API-запросы (curl с реальным логином) — создание с подзадачами, блокировка/разблокировка done, добавление/удаление подзадачи к существующей задаче, каскадное удаление — всё работает корректно. Не удалось прогнать через браузер (Playwright тулинг недоступен в этой среде — ни `playwright-cli`, ни `uv` не установлены).
- **2026-07-10**: вкладка "Поручил" в разделе "Задачи для проверки" — админ видит все задачи, которые лично создал и назначил сотрудникам (`created_by = я`, есть исполнитель), любой статус, дефолт-фильтр "все кроме готовых". Раньше приходилось искать вручную в "Все задачи". Backend: `GET /api/tasks/assigned-by-me` (server.js, рядом с `pending-review`) — та же схема прав (`admin || review_tasks`), исключает задачи без исполнителя, опциональный `?status=`. Frontend: `renderReviewPage()` стал диспетчером двух вкладок (`_reviewTab` state в sessionStorage, по образцу `_finTab`), переиспользует существующие `taskCard()`, `.fin-tab`/`.team-tabs-bar` (tab-бар), `.filter-btn` (статус-фильтр) — новых CSS-классов и цветов не вводили. Badge в сайдбаре (`updateReviewBadge`) не трогали — остаётся считать только pending_review, это отдельный action-inbox сигнал. Решения по продукту: задачи без исполнителя не показываем; мульти-исполнитель с самим админом внутри — показываем; группировка по сотруднику не нужна (хватило плоского списка + фильтра).
  - **Баг при первой проверке**: после правки `public/js/app.js` забыл бампнуть `?v=N` у `app.min.js` в `index.html` (собрал бандл, но не поднял версию). `Cache-Control: public, max-age=31536000, immutable` на статику — браузер держит старый JS по тому же URL вечно, обычный F5-рефреш не помогает (только явный hard-reload/смена версии). Симптом: таб-бар "Поручил" пропадал после рефреша, хотя сразу после правки в текущей вкладке всё работало (SPA-навигация без реального reload). Правило на будущее: **при любой правке `public/js/app.js` или `public/css/style.css` — сразу бампать версию в index.html в том же шаге, что и `node build.js`, не откладывать**.
- **Timesheet**: колонки "Премия" и "Аванс" — добавлены в БД, сервер, таблицу и форму.
- **Комментарии**: автокомплит по `@` (упоминания), панель эмодзи (кнопка 😊).

## UI-договорённости

- Диалоги удаления — только кастомная модалка в стиле приложения, никогда `confirm()`. Применено везде в кодовой базе (2026-07-07): все 16 мест с нативным confirm() заменены на showConfirmModal().
- Клик по оверлею модалки должен закрывать её только если `mousedown` начался на самом оверлее (не внутри контента) — иначе клик-драг из модалки наружу закрывает её случайно.
- `.btn-primary` требует `border: 1.5px solid transparent`, иначе высота не совпадает с `.btn-outline`.

## Кэш и деплой

- CSS **и JS** кэшируются через `?v=N` в `index.html` — бампать версию при каждом деплое изменений соответствующего файла, иначе правки не долетают до браузеров (assets кэшируются `immutable` на год). Текущие: `style.min.css?v=39`, `app.min.js?v=25`.
- **Билд-шаг (с 2026-07-08)**: `public/js/app.js`/`public/css/style.css` — исходники, `index.html` грузит минифицированные `.min.js`/`.min.css` (esbuild, `npm run build`). После правки исходников — обязательно ребилд перед деплоем, иначе браузер видит старую минифицированную версию.
- Подробности деплоя — см. `DEPLOY.md`.
- Домен `bar.mindstech.io` подключён к Railway (CNAME + TXT verify записи в DNS mindstech.io).

## Инфраструктура

- SSE (`/api/events`) должен быть исключён из `compression()` middleware — gzip буферизует stream и рвёт realtime-доставку событий. Если добавляются новые global middleware в server.js — проверять, что они не оборачивают SSE-роут.
- **PWA**: manifest.json + иконки (192/512/apple-touch) готовы, устанавливается на Android/iOS. Мобильный safe-area (iPhone home indicator) требует `viewport-fit=cover` в viewport meta — без него `env(safe-area-inset-*)` всегда 0.
- **Desktop**: `desktop/` — Electron-обёртка, грузит прод-URL напрямую (не bundled статику). Обновления сервера подхватываются сами; пересборка `.dmg`/`.exe` (`cd desktop && npm run build`) нужна только при изменении самого `desktop/main.js`. `desktop/dist/` — build-артефакт, в `.gitignore`, не коммитится.
- **SQLite `LOWER()` — ASCII-only**, не лоуеркейзит кириллицу. Для case-insensitive сравнения кириллического текста в SQL использовать `lower_u()` (custom function, зарегистрирована в `database.js` через `db.function()`, backed by JS `.toLowerCase()`), не встроенный `LOWER()`.
- **Задачи**: одиночный исполнитель = обычный `tasks.status`, 2+ исполнителя = `task_assignees` (чеклист-формат, `PATCH .../my-done`). Любое место, меняющее статус на `done` (PUT, my-done, approve), должно вызывать `spawnRecurringTask()` — три существующих места уже это делают, если добавляется новый способ завершить задачу, не забыть про recurring. Detail-модалка рендерит РАЗНЫЙ UI в зависимости от того, есть ли хоть одна строка в `task_assignees` для задачи (чеклист-toggle рядом с аватаркой) или нет (обычная кнопка "Готово" рядом с Редактировать) — если видишь такое расхождение между двумя визуально похожими задачами, проверь `multi_assignees.length`, не баг рендера. Кнопка "Готово"/toggle должна быть видна админу для ЛЮБОЙ задачи, не только своей — обе ветки (single и multi) обязаны включать `isAdmin` в условие показа.
- **Календарь**: повторяющиеся события — одна строка в БД, occurrences считаются на лету (`_calExpandRecurring`) и **все делят один и тот же `id`**. Любой код, работающий с конкретным occurrence (detail modal, delete, edit), обязан матчить по `id` **и** `start.dateTime` вместе — иначе всегда резолвится первое вхождение серии.
- **CORS**: применяется только к `/api/*` (с 2026-07-08), не глобально — статика (шрифты/JS/CSS) идёт мимо CORS-проверки, т.к. браузер шлёт font `url()` в cors-режиме даже same-origin, и глобальный CORS блокировал их без Origin в allowlist. Ограничен списком `ALLOWED_ORIGINS` в server.js (bar.mindstech.io + railway domain + localhost:3000/127.0.0.1:3000 только если `NODE_ENV!=='production'`). Запросы без Origin header (curl, Electron same-origin) всегда проходят. Новый клиентский домен — добавить в список.
- **Дедлайны date-only** ("2026-07-08", без времени) трактуются как конец дня 23:59:59 — и на фронте (`parseDeadline()`, app.js), и в scheduler.js. Если где-то сравниваешь `deadline` с текущим временем — не сравнивай голую дату-строку напрямую, для date-only дедлайнов сначала приведи к `deadline + 'T23:59:59'`, иначе сравнение матчит "весь день" вместо "осталось N часов".
- **Double-submit**: формы с async-сохранением (`saveCalEvent`, `saveEditCalEvent`) обязаны дизейблить кнопку submit на время запроса — без этого быстрый повторный клик создаёт дубли (наблюдалось на календарных событиях: 2 одинаковых события + 2 отдельных telegram-уведомления от одного клика). Паттерн: `id` на кнопке, `if (btn.disabled) return; btn.disabled = true;` перед запросом, `btn.disabled = false` только в catch. Другие save-формы в кодовой базе не аудировались на этот баг — если жалуются на дубли при сохранении чего-либо, проверить первым делом.
- **Транзакции**: `db.transaction(fn)` доступна в database.js (better-sqlite3-style). Используется на project/task delete каскадах. Новые многошаговые DELETE/UPDATE — оборачивать так же.
- **Telegram**: `TELEGRAM_WEBHOOK_SECRET` опционален — если не задан в Railway, webhook работает без проверки secret token (backward-compat). Установить переменную = включить проверку без доп. кода.

## Лог изменений

(новые записи сверху)

- 2026-07-08 — fix: 6 багов после perf-аудита (найдены по живым жалобам юзера на проде), 6 коммитов, все задеплоены и подтверждены на bar.mindstech.io. (1) c3197ae — табель всё ещё дублировался (455 строк на проде вместо 17): предыдущий UNIQUE index (в этот же день) не мог создаться, т.к. данные уже были задублированы до его добавления — ошибка тихо глоталась в catch{}. One-time дедуп в initDB() (оставляет строку с наибольшим числом timesheet_records) ПЕРЕД CREATE UNIQUE INDEX. (2) 5c26747 — шапка на iPhone 17 Pro Max уходила под статус-бар/Dynamic Island: `black-translucent` статус-бар требует `padding-top: env(safe-area-inset-top)` на `.topbar`, которого не было. (3) 548f3cd — telegram-уведомление "меньше часа до дедлайна" приходило в любое время дня для date-only дедлайнов (сравнение голых date-строк матчило весь день) — теперь сравнивается конец дня 23:59:59, как и фронтенд уже считает. (4) a605749 — admin не мог отметить "Готово" за чужую single-assignee задачу (условие показа кнопки не включало isAdmin, хотя мульти-assignee ветка это умела). (5) 899f9a5 — 157 задач на проде имели осиротевшую 1-строчную запись в task_assignees (легаси с эпохи до single-vs-multi разделения), из-за чего рендерился чеклист-UI вместо обычного "Готово"-UI для одного и того же типа задачи — one-time cleanup удаляет task_assignees там, где ровно 1 запись на task_id. (6) 9a7ee04 — двойной клик "Сохранить" на календарном событии создавал 2 события + 2 telegram-уведомления — кнопка теперь дизейблится на время запроса. style.min.css?v=39, app.min.js?v=25.
- 2026-07-08 — perf: полный аудит производительности + PWA + новые виды календаря. Backend: CORS сужен до /api (глобальный CORS блокировал same-origin font-запросы — обнаружено при внедрении Gotham-шрифта), async bcrypt (было sync, блокировало event loop), N+1 в content-plan sync (batched deletes + shared project/members cache), 3 индекса (finance_history/push_subscriptions/finance_activity_log), TTL-кэш на dashboard/reports/best-employee/workload (20-30с, корректно scoped org vs per-user), LIMIT 2000 на /api/tasks?all=1. **Баг табеля**: timesheet_employees дублировался на каждой загрузке страницы (INSERT OR IGNORE без UNIQUE constraint на user_id — 97 строк вместо 18), почищено + partial UNIQUE INDEX добавлен, регрессия проверена (3 повторных вызова, всё ещё 18). Frontend: esbuild минификация (новый build.js, npm run build — index.html теперь грузит .min.js/.min.css, не исходники), Gotham 8×OTF/TTF (~1MB) → 2×WOFF2 (~106KB) через fonttools, debounce на task-search, PWA iOS meta+maskable icon+SW cache-first. Календарь: Day/4day/Week (единый time-grid renderer с новым overlap-алгоритмом, не переиспользует Month'овский assignSlot), Year (12 мини-месяцев), Agenda (список) — все 6 видов через один _calViewMode switch, независимый от старого _calTab (Календарь/Дежурства/Расписание). Задеплоено (commit b3049ce), проверено на bar.mindstech.io.
- 2026-07-07 — fix: полный security-аудит (16 находок), 4 коммита. Критично: IDOR в GET /api/tasks/:id (нет canAccessTask), XSS через title/name в 12 местах, удалён мёртвый seed-эндпоинт с реальными bcrypt-хешами 16 аккаунтов из исходников (git history не чистили — пароли стоит сменить отдельно). Важно: JWT_SECRET fatal вместо fallback, JWT 30d→7d, global error handler, DELETE user не падает с FK error, mobile touch targets 44px. Желательно: CORS ограничен, Telegram token TTL+webhook secret, транзакции на каскадных удалениях, 5 индексов добавлено. Cleanup: мёртвый Google OAuth код + googleapis зависимость удалены. Не сделано (по решению юзера): git history rewrite, GET /api/users permissions restriction, JWT invalidation по смене пароля, доп. mobile breakpoints (нужен визуальный проход). Полный отчёт был в отдельном Artifact.
- 2026-07-07 — fix: удаление occurrence повторяющегося календарного события ("это и все последующие") удаляло всю серию — openCalEventDetail() матчил только по id (все occurrences делят один id), всегда резолвя первое вхождение вместо того на которое кликнул юзер. Теперь матчит по id+start.dateTime.
- 2026-07-07 — fix: повторяющиеся задачи не спавнили следующую через checklist/approve пути (только через обычный PUT-статус) — вынесено в shared spawnRecurringTask(), вызывается из всех трёх мест. Root cause: одиночный исполнитель ошибочно создавал task_assignees запись — setTaskAssignees() теперь только для 2+ исполнителей. Побочный эффект пришлось чинить: одиночные задачи потеряли единственный способ отметить "готово" (кнопка рендерилась только для мульти) — добавлена toggleTaskDone() + кнопка в карточке/detail. Detail-модалка теперь закрывается сама при финальном статусе (done/pending_review), но не при частичном мульти-завершении. deleteTask/save-задачи теперь также обновляют страницу "Мои задачи" (раньше только "Все задачи").
- 2026-07-07 — fix: описание задачи/календарного события теряло переносы строк (нет white-space:pre-wrap на detail-view). Заодно task description теперь экранируется (_escHtml) — раньше вставлялся сырым, XSS-риск. Отдельно: убраны последние нативные confirm() (16 мест) в пользу showConfirmModal() — finance/kids/b2c удаление курса/студента, задачи, проекты, content-plan, расписание, feedback, telegram disconnect.
- 2026-07-06 — fix: глобальный поиск не находил задачи с заглавными кириллическими буквами (SQLite LOWER() не работает с не-ASCII). Зарегистрирована lower_u() unicode-aware функция, используется в /api/search вместо LOWER(). Проверено: заглавные↔строчные кириллица, смешанный регистр, ASCII без регрессии.
- 2026-07-06 — fix: мобильные модалки не реагировали на скролл — body scroll не блокировался при открытии, touch проваливался на контент под модалкой. openModal()/closeModal()/confirmDel() теперь блокируют/восстанавливают body.style.overflow. Добавлен -webkit-overflow-scrolling:touch на .modal. Заодно бампнуты cache-busting версии (app.js v=9, style.css v=29) — без этого правки не долетали до реальных браузеров.
- 2026-07-06 — fix: PWA-иконки (192/512/apple-touch) заменены на wordmark "minds bar" — старый equalizer-логотип заменён по просьбе юзера. Кроп: bbox текста + равный паддинг вокруг, центрировано на bbox (не на геометрический центр исходника — тот сам смещён вниз в оригинальном файле).
- 2026-07-06 — feat: PWA install-иконки (192/512/apple-touch), iOS safe-area fix (viewport-fit=cover + .page-content padding учитывает safe-area-inset-bottom), Electron desktop-обёртка (desktop/).
- 2026-07-06 — fix: SSE-события задерживались из-за глобального compression() middleware, буферизующего /api/events (задачи "на проверку" появлялись только после re-render страницы, не в реальном времени). Исключён /api/events из сжатия, refreshCurrentPage() теперь также освежает review-страницу при SSE reconnect.
- 2026-07-06 — fix: раздел "Задачи на проверку" не получал задачи для менеджеров без admin-роли (право review_tasks игнорировалось, gate проверял только role==='admin'). Три места в server.js: PUT /api/tasks/:id, PATCH /api/tasks/:id/my-done, добавлен helper userCan(). Подтверждено на реальном юзере (Пулатова Камилла, id=2).
- 2026-07-06 — добавлены DEPLOY.md, MEMORY.md, CLAUDE.md с правилом session-end логирования

---
Последнее обновление: 2026-07-08 (6 post-audit фиксов, commit 9a7ee04 — confirmed online на bar.mindstech.io)
